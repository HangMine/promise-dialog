{
  "name": "promise-dialog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepare": "pnpm build && pnpm build:react",
    "start": "pnpm --filter promise-dialog-vue-doc dev",
    "start:react": "pnpm --filter promise-dialog-react-doc dev",
    "build": "pnpm --filter promise-dialog-script build && pnpm --filter promise-dialog-vue build",
    "build:react": "pnpm --filter promise-dialog-script build && pnpm --filter promise-dialog-react build",
    "docs:dev": "pnpm --filter promise-dialog-vue-doc docs:dev",
    "docs:build": "pnpm --filter promise-dialog-vue-doc docs:build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "~5.2.0"
  }
}
